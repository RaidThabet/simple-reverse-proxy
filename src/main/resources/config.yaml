server:
  port: 8080
  ratelimit:
    maxTokens: 20
    refillRatePerSecond: 20

routes:
  - prefix: /api/order
    upstream:
      host: localhost
      port: 8081
  - prefix: /api/product
    upstream:
      host: localhost
      port: 8082